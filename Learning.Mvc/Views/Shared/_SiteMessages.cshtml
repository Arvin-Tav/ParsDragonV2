@if (
TempData["SuccessMessage"] != null ||
TempData["ErrorMessage"] != null ||
TempData["InfoMessage"] != null ||
TempData["WarningMessage"] != null
)
{
    if (TempData["SuccessMessage"] != null)
    {
        <script>
            $(document).ready(function() {
                ShowMessage('پیغام موفقیت', '@TempData["SuccessMessage"]', 'success');
            });
        </script>
    }

    if (TempData["ErrorMessage"] != null)
    {
        <script>
            $(document).ready(function() {
                ShowMessage('پیغام خطا', '@TempData["ErrorMessage"]', 'error');
            });
        </script>
    }

    if (TempData["WarningMessage"] != null)
    {
        <script>
            $(document).ready(function() {
                ShowMessage('پیغام اخطار', '@TempData["WarningMessage"]', 'warning');
            });
        </script>
    }

    if (TempData["InfoMessage"] != null)
    {
        <script>
            $(document).ready(function() {
                ShowMessage('پیغام اطلاعیه', '@TempData["InfoMessage"]', 'info');
            });
        </script>
    }
}

@if (
TempData["SuccessSwalMessage"] != null ||
TempData["ErrorSwalMessage"] != null ||
TempData["InfoSwalMessage"] != null ||
TempData["WarningSwalMessage"] != null
)
{

    if (TempData["SuccessSwalMessage"] != null)
    {
        <script>
            $(document).ready(function() {
                ShowSwalMessage('پیغام موفقیت', '@TempData["SuccessSwalMessage"]', 'success');
            });
        </script>
    }

    if (TempData["ErrorSwalMessage"] != null)
    {
        <script>
            $(document).ready(function() {
                ShowSwalMessage('هشدار', '@TempData["ErrorSwalMessage"]', 'error');
            });
        </script>
    }

    if (TempData["WarningSwalMessage"] != null)
    {
        <script>
            $(document).ready(function() {
                ShowSwalMessage('هشدار', '@TempData["WarningSwalMessage"]', 'warning');
            });
        </script>
    }

    if (TempData["InfoSwalMessage"] != null)
    {
        <script>
            $(document).ready(function() {
                ShowSwalMessage('پیغام اطلاعیه', '@TempData["InfoSwalMessage"]', 'info');
            });
        </script>
    }
}

